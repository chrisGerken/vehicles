package com.gerkenip.vehicles.model;

/**
 * Type of static simulation object.
 */
public enum ObjectType {
    /** Point light source at (x, y) with specified radius */
    POINT,

    /** Line segment from (x, y) to (x2, y2) */
    WALL
}
